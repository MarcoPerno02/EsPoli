clc
clear all

format longG


n = 3;
i = linspace(1, 3, 3);
z = ones(3,1);
for idx=1:3
    z(idx) = -cos(((2.*i(idx)-1).*pi)./(2.*n));
end
x = ((2*pi)/2)*z+(2*pi/2);

pi*(1+3^(1/2)/2);

%%%%%%%%
f = @(x) sin(5*x);
x = [0, pi/10, pi/5];
y = f(x);
c = polyfit(x, y, 2);
polyval(c, pi/20);

%%% LUx = Pb
A = [4, 4; -2, 8];
norm(A, 2);

%%%
R = [1, 1, 0, 3;0, 2, 4, 4;0, 0, 2, 7;0,0,0,3];
A = R'*R;
det(A)

%%%
A = [3,0,1;2,-1,0;5,1,1;0,1,2];
[Q, R] = qr(A);
%%%%

212.41*50.326;
%%%

x = [-1;2];
A = [1,2;0,7];
(x'*A*x)/(x'*x);


A = [-2*(2)^(1/2), 2, 1;2, 2*(2)^(1/2), -1; 1, -1, 1];
rank(A);

%%%% 
A = [2,0,1,4;0,1,0,2;1,-1,0,3];
N = [1,-1,0,3;0,1,0,2;1,0,1,-1];
ris = N*A*N^-1